```bash
#creates git repo
git init

#priints all commits on one line
git logs -- oneline

#sync with online
git remote odd origin http://usr/example.git

#install all deps in package.json
npm install

#gitignore
touch .gitignore
```